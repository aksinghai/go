package all.company.com.MachineCoding.Tinder;

public enum Command {
    create_account,
    delete_account,
    potential_match,
    like,
    ignore,
    show_match,
    show_all_matches,
    exit;
}
